# nape-hud 使い方

Keychron Nape Pro の **レイヤー / OctaShift の向き / DPI** を検出し、
ボタンを押した瞬間に画面隅へ数秒間ポップアップ表示する macOS 常駐ツール。

接続は **Bluetooth / 2.4GHz ドングル / USB 有線** のどれでも動く。
接続を切り替えても自動で追従し、いま何で繋がっているかもポップアップに出る。

> 技術的な背景（HID レポートの解析結果、リバースエンジニアリングの経緯）は
> [README.md](README.md) に分けてある。この文書は日常の使い方だけを扱う。

---

## 1. インストール（アプリとして使う）

### インストーラ (.pkg) を使う（かんたん）

```bash
cd ~/Desktop/nape-hud
./scripts/make-pkg.sh
open build/          # 出来た NapeHUD-1.0.0.pkg を開く
```

pkg が入れるもの:

| 対象 | 内容 |
|---|---|
| `/Applications/NapeHUD.app` | アプリ本体 |
| `~/.config/nape-hud/config.json` | **無い場合のみ**作成（既存の設定は温存） |
| `~/.config/nape-hud/config.example.json` | 雛形（毎回更新） |
| インストール後 | アプリを自動起動 |

> ⚠️ **未署名のため初回は警告が出る。**
> pkg を**右クリック →「開く」**で進めてください（ダブルクリックでは弾かれます）。
> ターミナルからなら `sudo installer -pkg build/NapeHUD-1.0.0.pkg -target /` でも入る。
> Developer ID Installer 証明書があれば署名できる:
> `PKG_SIGN_IDENTITY="Developer ID Installer: ..." ./scripts/make-pkg.sh`

インストール中に稼働していたアプリは preinstall で自動的に終了させる。

### 手動で入れる

```bash
cd ~/Desktop/nape-hud

# 1. 設定ファイルを配置
mkdir -p ~/.config/nape-hud
cp config.example.json ~/.config/nape-hud/config.json

# 2. アプリをビルドして /Applications へ入れる
./scripts/make-app.sh --install

# 3. 起動
open /Applications/NapeHUD.app
```

`build/NapeHUD.app` に置くだけなら `--install` を省く。

起動すると **メニューバーに常駐**する（Dock には出ない）。
アイコンの右に現在のレイヤー番号が出る。クリックでメニューが開く:

| メニュー項目 | 内容 |
|---|---|
| （上部 3 行） | 現在のレイヤー / 向き / DPI / 接続。設定漏れの警告もここに出る |
| いまの状態を表示 | 直前のポップアップを出し直す |
| HUD のテスト表示 | 見た目の確認 |
| **ポインタ加速** | **加速の入り切り（有効時のみ表示・§7）** |
| **キーアサインを表示…** | **デバイスに登録されたキーマップを別画面で表示（§6）** |
| **向きを校正…** | **8 方向の校正（§5）。結果を設定ファイルへ自動反映できる** |
| **設定…** | **設定画面（⌘,）。表示・ポインタ加速・レイヤー名を GUI で編集** |
| 設定ファイルを開く | `~/.config/nape-hud/config.json` をエディタで開く（全項目） |
| 設定を再読み込み（再起動） | 設定変更を反映する |
| nape-hud を終了 | 終了 |

### ログイン時に自動起動する

**システム設定 → 一般 → ログイン項目** に `NapeHUD.app` を追加するのが一番簡単。

落ちたときに自動復帰させたい場合は LaunchAgent を使う:

```bash
cp com.local.nape-hud.plist ~/Library/LaunchAgents/
# plist 内の実行パスを実際の場所に合わせて編集してから:
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.local.nape-hud.plist
```

解除するとき:

```bash
launchctl bootout gui/$(id -u)/com.local.nape-hud
```

ログは `/tmp/nape-hud.log`（plist で変更可）。

### CLI として使う

同じ実行ファイルが CLI も兼ねている。診断や校正はこちらから行う。

```bash
/Applications/NapeHUD.app/Contents/MacOS/NapeHUD doctor
```

短く打ちたい場合はエイリアスを張る:

```bash
echo 'alias nape-hud="/Applications/NapeHUD.app/Contents/MacOS/NapeHUD"' >> ~/.zshrc
```

アプリ化せずビルドしたバイナリを直接使うこともできる（`swift build -c release` →
`./.build/release/NapeHUD run`）。この場合はメニューバーは出るが、
権限はアプリではなく**ターミナルに紐づく**点に注意（§7）。

---

## 2. 何が表示されるか

3 つのボタンそれぞれで、押したものが**主役**になり、残り 2 つは補助行に並ぶ。

| 押したボタン | 主役表示 | 補助行 |
|---|---|---|
| レイヤー切替 | `Layer 3` / レイヤー名 | 向き · DPI · 接続 |
| 8 方向切替 | `OctaShift` / `90°` | レイヤー · DPI · 接続 |
| DPI 切替 | `DPI` / `1600 DPI` | レイヤー · 向き · 接続 |

表示イメージ:

```
┌────────────────────────────────────┐
│   ╱│╲     LAYER 3                  │
│  (  ↑ )   Media                    │
│   ╲│╱     90°  ·  1600 DPI  ·  BT  │
└────────────────────────────────────┘
```

左の円は **45° 刻み 8 方位のダイヤル**で、針が現在の向きを指す。

接続/切断時にも `Nape Pro / 接続: USB` のようなポップアップが出る
（`hud.showOnConnectionChange: false` で無効化できる）。

---

## 3. 表示のカスタマイズ

### 設定画面から（かんたん）

メニューバー → **「設定…」**（⌘,）。よく触る項目を GUI で編集できる。

| タブ | 内容 |
|---|---|
| 表示 | 表示秒数・位置・拡大率・レイヤー番号の表記・各種チェックボックス |
| ポインタ加速 | 有効/無効・効き始め・最大速度・最大倍率（**カーブの図つき・即時反映**） |
| レイヤー名 | 各レイヤーの呼び名 |

「保存」で `config.json` に書き戻す。**該当する値だけを置換する方式**なので、
ファイル内のコメントや整形は崩れない（元ファイルは自動でバックアップ）。
保存時に JSON として壊れていないか検算し、壊れる場合は書き込まない。

表示まわりは再起動で反映（保存後に確認ダイアログが出る）。
**ポインタ加速の値だけはスライダーを動かした時点で即反映**される（効き具合を試すため）。

### 設定ファイルを直接編集する（全項目）

設定画面に無い項目（復号ルール・デバイス照合・校正・キーマップ・センチネルキーなど）は
`~/.config/nape-hud/config.json` を直接編集する。
メニューバー →「設定ファイルを開く」で開ける。

**書きたい項目だけ書けばよい**（残りは既定値）。
変更後はメニューバー →「設定を再読み込み（再起動）」で反映する。

### 見た目・位置

```json
"hud": {
  "seconds": 2.2,
  "position": "topRight",
  "margin": 28,
  "scale": 1.0,
  "showConnection": true,
  "showOnConnectionChange": true,
  "showAngleDial": true,
  "followsMouseScreen": true
}
```

| 項目 | 意味 |
|---|---|
| `seconds` | 表示秒数 |
| `position` | `center` `top` `topLeft` `topRight` `bottom` `bottomLeft` `bottomRight` |
| `margin` | 画面端からの余白（px） |
| `scale` | 全体の拡大率。`1.5` などで大きくできる |
| `showConnection` | 接続種別バッジ（`USB-C` / `2.4G` / `BT`）を出す |
| `showOnConnectionChange` | 接続・切断時にも通知する |
| `showAngleDial` | 角度ダイヤルを描く |
| `followsMouseScreen` | マウスがある画面に出す（複数ディスプレイ用） |
| `showMenuBarIcon` | メニューバー常駐項目を出す（`false` にすると終了操作ができなくなる） |
| `menuBarShowsLayer` | メニューバーに現在のレイヤー番号を添える |
| `layerNumberOffset` | レイヤー番号の表示補正。`-1` で Launcher の 0〜7 に揃う（既定 `0` = 1〜8） |

### レイヤーに名前を付ける

レイヤーは全 8 段。本ツールは状態通知どおり **1〜8** で表示する
（Launcher は同じレイヤーを 0〜7 と表示する。揃えたい場合は
`hud.layerNumberOffset` を `-1` にして、下のキーも 0〜7 に書き換える）。

```json
"layerNames": {
  "1": "Base",
  "2": "Scroll",
  "3": "Media",
  "4": "Edit"
}
```

### 向きに名前を付ける

キーは角度（度数）。省略した角度は `135°` のように度数だけ表示される。

```json
"angleNames": {
  "0": "0° 横置き",
  "90": "90° 縦置き",
  "180": "180° 反転"
}
```

### DPI の実値を設定する ⚠️ 要確認

デバイスは **段番号（0〜4）しか返さない**ため、実 DPI 値への変換表が必要。
同梱の設定には仮の値が入っている:

```json
"dpi": {
  "offset": 3,
  "map": { "0": 800, "1": 1200, "2": 1600, "3": 2400, "4": 3200 }
}
```

**この値は未確認**。公式スペックの「DPI 800〜3200 可変」から標準的な 5 段を仮置きしたもので、
段ごとの実値は公開されていない。**Keychron Launcher の DPI 設定で実際の値を確認して
書き換えること。** 段数が 5 でなかった場合もここに追記すれば対応できる。

対応表に無い段を受信すると、ポップアップには `段 0x05（DPI 値 未設定）` と出て、
起動元の端末にも警告が出るので取りこぼしに気づける。

DPI に呼び名を付けたい場合は `dpiNames`（任意）:

```json
"dpiNames": { "800": "精密", "3200": "高速" }
```

### 向きの基準を変える

同梱の設定は向きコード `0x00` を 0° としている。別の向きを基準にしたい場合は
`calibration.angleZeroCode` を書くか（§5-3）、`rules[].angle.map` を直接ずらす。

```json
"angle": {
  "offset": 2,
  "map": { "0": 0, "1": 45, "2": 90, "3": 135, "4": 180, "5": 225, "6": 270, "7": 315 }
}
```

アプリの校正画面で選び直すのが簡単（§5）。

---

## 4. コマンド一覧

| コマンド | 用途 |
|---|---|
| `nape-hud` / `nape-hud run` | 常駐して切替をポップアップ表示（通常これだけ使う） |
| `nape-hud doctor` | 権限・設定・デバイス検出の健診 |
| `nape-hud test` | HUD の見た目をダミー表示で確認（実機不要） |
| `nape-hud devices` | 検出したインターフェースと接続種別を一覧 |
| `nape-hud calibrate` | 8 方向切替ボタンを 1 周させて向き → 角度の対応表を作る（アプリのメニューからも可） |
| `nape-hud keymap` | デバイスに登録されたキーアサインを表示（アプリのメニューからも可） |
| `nape-hud selftest` | 合成レポートで復号経路と設定書き戻しを検証（実機不要・原因切り分け用） |
| `nape-hud learn` | 誘導付き学習でレポート形式を再特定（ファーム更新後など） |
| `nape-hud sniff` | 生 HID レポートを観測（詳細解析用） |

### オプション

| オプション | 意味 |
|---|---|
| `-c, --config <path>` | 設定ファイルを指定（既定 `~/.config/nape-hud/config.json`） |
| `-d, --debug` | `run` で一致レポート全文と復号結果を出力 |
| `-s, --seconds <n>` | `sniff` / `calibrate` / `learn` の観測秒数（既定 90 秒、`0` で無制限） |
| `-v, --verbose` | `sniff` で全レポートを表示 |
| `-p, --pointer` | `sniff` / `learn` でキーボード・ポインタ面も含める |

> `sniff` と `calibrate` は既定 90 秒で自動終了して結果を出す。
> タイムアウトのある環境から実行しても結果を取り逃さない。

---

## 5. 向きの校正

実際の向きと表示がずれているとき（＝ 0° の基準を変えたいとき）に使う。
**アプリからでも CLI からでも**できる。

### アプリから（推奨）

メニューバー → **「向きを校正…」**

1. **Keychron Launcher を閉じる**（開いていると通信が混ざって精度が落ちる）
2. **どの向きから始めてもよい。** 8 方向切替ボタンを 8 回押して 1 周させる
3. 押すたびに `#1 0x01 → 45°` のように検出結果が並ぶ
4. 8 方向そろうと自動で完了する
5. **「0° にする向き」** を選ぶ（§5-3）
6. **「設定に保存」** を押すと `config.json` の `angle.map` が書き換わる

| 操作 | 動作 |
|---|---|
| 0° にする向き | 検出した向きから 0° の基準を直接選ぶ |
| ◀ ▶ | 0° の基準を一覧の前後へ 1 つずらす |
| 設定に保存 | `config.json` を更新（**元ファイルは自動でバックアップ**）。続けて再起動できる |
| JSON をコピー | `map` をクリップボードへ。手で貼りたいとき用 |
| ここまでで確定 | 1 周させずに、押した分だけで確定する |
| キャンセル | 何も変更せず閉じる |

書き換えは `angle` の `map` の**その 1 行だけ**を置換する方式なので、
設定ファイルのコメントや整形は崩れない。
`rules[].angle` の位置を特定できなかった場合は**ファイルを変更せず**、
手で貼るための JSON を提示する。

> 校正中は通常のポップアップを止める（ボタンを押すたびに HUD が出ると邪魔なため）。
> 終了すると自動的に通常動作へ戻る。

### CLI から

```bash
nape-hud calibrate
```

手順は同じ。最初の向きに戻った時点で自動終了し、貼り付け用の `map` が出力される。
1 周しなかった場合も既定 90 秒（`-s` で変更）で打ち切る。
出力を `config.json` の `rules[].angle.map` に手で反映する。

### 5-3. どの向きから始めるか（`calibration` 設定）

校正は**任意の向きから始められる**。0° の基準は次の優先順で決まる。

| 優先 | 決め方 |
|---|---|
| 1 | 校正画面の「0° にする向き」で選んだもの |
| 2 | 設定 `calibration.angleZeroCode`（生コード 0〜7） |
| 3 | 現在の `rules[].angle.map` で 0° になっているコード（前回の校正結果を引き継ぐ） |
| 4 | **開始時の向き**（何も手掛かりが無いときの既定） |

```json
"calibration": {
  "positions": 8,          // 1 周あたりの方向数（OctaShift は 8）
  "angleZeroCode": null,   // 0° の基準にする生コード。書けばどの向きから始めてもよい
  "clockwise": true        // false にすると押す向きに対して角度が減る
}
```

`angleZeroCode` を書いておけば、**毎回それが初期値になる**ので
「0° にしたい向きから始める」必要がなくなる。
校正画面はどの基準が使われたかを `基準: 設定 calibration.angleZeroCode` のように表示する。

> **既定（優先 4）の根拠**
> デバイスは「ボタンを押した**後**の向き」を通知するため、最初に検出される向きは
> 開始時の向きの *次* になり、1 周ぶん押し終えたときの**最後の検出が開始時の向き**になる。
> そのため基準の手掛かりが無い場合は「最後の検出 = 開始時の向き」を 0° とする。

回転方向が実際と逆に感じる場合は `clockwise` を `false` にする
（押す向きに対して 45° ずつ減る割り当てになる）。

### 共通の注意

ボタンは決まった順に巡回するので、押した順がそのまま向きの並び順になる。
角度は「検出数で 360° を割った等間隔」で割り当てられるため、
**1 周させずに確定すると刻み幅がずれる**（8 方向なら 8 個検出させる。
4 個で止めると 90° 刻みになってしまう）。
実際の向きと合わない場合は `map` の右辺（度数）を手で直せばよい。

---

## 6. キーアサインの表示

デバイスに登録されているキーマップを読み出して一覧表示できる。

**アプリから**: メニューバー → 「キーアサインを表示…」
**CLI から**: `nape-hud keymap`

```
Keychron Nape Pro キーアサイン
VIA プロトコル版数 12 / レイヤー 9 / マトリクス 1 行 × 7 列 / ダイヤル 1 個 / マクロ 16 スロット（未使用）
キー数は座標指定の読み出し（0x04）と一致を確認済み

レイヤー        col0        col1        col2        col3        col4        col5        col6        ダイヤル↻  ダイヤル↺
0: Layer 0      LYR:522A    ----        Btn4        ----        Btn1        Btn2        LYR:522B    Vol+        Vol-
1: Layer 1      KC:7E29     Btn1        KC:7E29     KC:7E29     KC:7E29     KC:7E29     LYR:522B    WhDn        WhUp
2: Layer 2      ⌘C          Btn1        ⌘V          Btn2        KC:7E29     KC:7E29     LYR:522B    WhDn        WhUp
…

全レイヤー共通のボタン（レイヤーに依らず同じ働き）
  col6 = LYR:522B
```

画面右下の「コピー」でテキストごとクリップボードへ、「再読み込み」で再取得できる。

### 仕組み

Nape Pro の raw HID（`0xFF60`）は **VIA プロトコル**の問い合わせに応答する（実機で確認済み）。

| コマンド | 内容 |
|---|---|
| `0x01` | プロトコル版数（12） |
| `0x11` | レイヤー数（9） |
| `0x0C` | マクロ数（16） |
| `0x12` | キーマップ読み出し（offset 2 バイト + 長さ） |
| `0x14` | ダイヤル（エンコーダ）の割り当て |

`Keychron Launcher を閉じてから`実行すること。開いていると応答を取り合ってしまう。

### 表示の読み方

| 表記 | 意味 |
|---|---|
| `----` | 未割り当て |
| `TRNS` | 下位レイヤーを透過 |
| `Btn1`〜`Btn5` | マウスボタン |
| `WhUp` / `WhDn` | ホイール上下 |
| `Vol+` / `Vol-` | 音量 |
| `⌘C` など | 修飾キー付きの組み合わせ |
| `LYR:xxxx` | レイヤー関連のキーコード。**正確な意味は未確認**のため 16 進で表示 |
| `KC:7Exx` | **Keychron 独自キーコード**。名称は非公開のため 16 進で表示 |

### 実機の仕様

| 項目 | 内容 |
|---|---|
| レイヤー | 全 8 段。状態通知は **1〜8**、Launcher の表示は **0〜7**（同じものの呼び方の違い） |
| OctaShift | 0° / 45° / 90° / 135° / 180° / 225° / 270° / 315° の 8 方向 |
| 割り当て可能 | **6 キー（M1 / M2 / 01 / 02 / 03 / 04）＋ ダイヤル上下** |
| 単押し / 長押し | 各キーは**単押しと長押しで別の動作**を割り当てられる（ユーザーが任意に設定可能） |
| 保持単位 | 割り当ては **レイヤー × 角度 × 単押し/長押し** の組み合わせごとに保持される |

Launcher の `右クリック | C(KC_UP)` のような `|` 区切りの表記が **単押し | 長押し** を表す。

### 分かっていないこと

- **角度別の割り当ては読めない**（実装していない）。VIA のキーマップ領域には
  角度別の内容が入っていないため、別のベンダコマンドを特定する必要がある。
  正確な内容は Launcher で確認すること。
- **物理ボタンとの対応**: マトリクス座標（1 行 × 7 列）は `0x04` の座標読みで確定したが、
  「col0 が M1 なのか 01 なのか」「単押し側か長押し側か」はデバイスから取得できない
  （VIA では配列情報をデバイスではなく定義ファイルが持つ）。
- **`0x7Exx` の意味**: OctaShift 切替や DPI 切替がここに該当すると思われるが、
  名称は公開されていない。観測値は `0x7E29` `0x7E2B` `0x7E2C`。
- **`0x522A` / `0x522B` の意味**: QMK のキーコード配置に当てると `MO(10)` / `MO(11)` に
  なるが、レイヤー数が 9 なので番号が合わない。誤った名前を出さないよう 16 進で表示している。
  `0x522B` は**全 9 レイヤーの col6 に共通**して置かれており、レイヤーに依らず働く
  ボタン（＝レイヤー切替ボタン）と考えられる。

`keysPerLayer` は自動判定（非ゼロのキーコード数 ÷ レイヤー数）したうえで、
座標指定の読み出し（`0x04`）と突き合わせて検算している。
一致しなかった場合は画面に警告が出るので、設定で固定する。

```json
"keymap": { "keysPerLayer": 7 }
```

## 7. ポインタ加速（任意機能・既定は無効）

小径トラックボールは 1 スワイプで稼げる移動量が小さい。
**速く転がしたときだけ**移動量を増幅して、大画面でも端まで届くようにする機能。
細かい操作の感触は保ったまま、大きく動かすときだけ効く。

ファームウェアではなくホスト側（カーソル移動イベントの書き換え）で実現しているので、
**文鎮化のリスクは無く、カーブをその場で調整できる**。

### 有効にする

```json
"acceleration": {
  "enabled": true,
  "onlyTrackball": true,
  "thresholdSpeed": 300,
  "fullSpeed": 2200,
  "baseGain": 1.0,
  "maxGain": 3.0,
  "maxDeltaPerEvent": 240,
  "perLayerGain": {}
}
```

| 項目 | 意味 |
|---|---|
| `enabled` | 既定 `false`。**`false` のままなら権限は一切不要**（今までどおり） |
| `onlyTrackball` | Nape Pro を操作しているときだけ効かせる。`false` で全ポインタ対象 |
| `trackballActiveWindowMs` | 「直前に動いた」とみなす時間（既定 200ms） |
| `thresholdSpeed` | 加速が効き始める速度（px/秒）。これ未満は等倍で細かい操作を保つ |
| `fullSpeed` | 最大倍率に達する速度（px/秒） |
| `baseGain` | しきい値未満での倍率。`1.0` が素の感度 |
| `maxGain` | 最大倍率 |
| `maxDeltaPerEvent` | 1 イベントあたりの移動量上限（px）。暴れ防止 |
| `perLayerGain` | レイヤーごとの最大倍率の上書き。例 `{"2": 1.0}` で細かい作業用レイヤーだけ切る |

`thresholdSpeed` 未満は `baseGain` 倍、`fullSpeed` 以上で `maxGain` 倍、
その間は smoothstep で滑らかにつなぐ。境目で急に変わらない。

カーブの数値は実機なしで確認できる:

```bash
nape-hud selftest
#  設定: 1.0x → 3.0x / 300〜2200 px/s
#  低速(250)=1.00x  中速(1250)=2.00x  高速(2700)=3.00x
```

**メニューバーから入り切りできる**（「ポインタ加速」項目）。
効き具合を試すときに便利。ここでの切替は設定ファイルには保存されない。

### 仕組み（なぜイベント書き換えではないのか）

macOS はカーソル位置を CGEvent より下層の IOHIDEvent から決めている。
そのため**イベントの座標や差分を書き換えてもカーソルは動かない**（実機で確認済み）。

| 試した方法 | 実効倍率 |
|---|---|
| セッション層のタップで `location` を書き換え | 1.07x（反映率 16%） |
| HID 層のタップで `delta` を書き換え | 1.00x |
| **元イベントを破棄して増幅した合成イベントを流し直す** | **3.01x** |

書き換え方式では「アプリに届く座標」だけがずれて経路がジグザグになり、
macOS のシェイク検出が働いて**カーソルが拡大するだけ**という挙動になった。
そのため最後の方式を採っている。

`run --debug` で実効倍率を確認できる:

```
加速: 2400 件（増幅 62%）/ 元 41230 px → 68715 px
     実効倍率 1.67x（全体平均）  増幅時の平均 2.08x  設定上限 2.10x
     到達した最高速度 3120 px/s（上限に達するのは 1654 px/s 以上）
```

**「実効倍率」は設定した最大倍率と一致しない**（一致してはいけない）。

| 数値 | 意味 |
|---|---|
| 設定上限 | `maxGain`。**最大になる速度に達したときだけ**の倍率 |
| 増幅時の平均 | しきい値を超えていたイベントの平均倍率 |
| 実効倍率（全体平均） | 素通しした遅い動きも含めた全体の平均。**必ず上限より低い** |

上限どおりの倍率が欲しい場合は「効き始め」を下げるか「最大になる速度」を下げる。
「到達した最高速度」が「最大になる速度」に届いていなければ、上限には一度も達していない。

### 効いていないときは

設定画面の加速タブに**実際の動作状況**が出る。設定値ではなく現在の状態なので、
ここが「動作中」でなければ効いていない。

| 表示 | 意味 |
|---|---|
| `● 動作中（1.0x → 3.0x / …）` | 効いている |
| `○ 停止中: アクセシビリティが未許可` | 許可が必要。ボタンから設定画面を開ける |
| `○ 停止中: 入力監視が未許可` | `onlyTrackball` に必要。許可するか `false` にする |
| `○ 停止中（設定で無効）` | チェックが外れている |

「動作中」なのに変化を感じない場合は、**効き始めの速度を下げる**か
**最大倍率を上げる**。効き始めが実際の操作速度より高いと、いつまでも等倍のままになる。

### ⚠️ 権限について

| `onlyTrackball` | 必要な権限 | 挙動 |
|---|---|---|
| `true`（既定） | アクセシビリティ **＋ 入力監視** | Nape Pro のときだけ加速 |
| `false` | アクセシビリティのみ | **内蔵トラックパッドにも加速がかかる** |

カーソル移動イベントには発生元デバイスの情報が無い（実機で確認したところ、senderID は
トラックボールでも内蔵トラックパッドでも同じ値だった）。そのため Nape Pro の HID 入力を
併せて監視し、直前に動きがあったかで判定している。これに入力監視が要る。

`onlyTrackball: true` のまま入力監視が未許可の場合、**トラックパッドへの誤爆を避けるため
加速を有効にしない**（メニューバーと起動ログに警告が出る）。

### ファームウェア改造について

トラックボール側（ファーム）で加速を実装する案は、現時点では**現実的ではない**。
Keychron は ZMK フォーク・Realtek HAL・Zephyr フォークを公開しているが、

- Nape Pro のボード定義が未公開（全リポジトリ検索で該当なし）
- PAW3222 センサのドライバが未公開
- 書き込み経路（DFU プロトコル）が非公開で、自前ビルドを流すと文鎮化のおそれ

将来 Keychron が公開すれば選択肢に入る。

---

## 8. 困ったときは

### ポップアップが出ない

```bash
nape-hud doctor     # デバイスが見えているか
nape-hud test       # HUD 自体が描画できるか
nape-hud selftest   # 復号ルールが正しいか
nape-hud run --debug   # 実機のレポートが届いているか
```

`--debug` で下記のような行が出れば、レポートは届いていて復号もできている。

```
[ルール名] A3 03 04 02 00 00 00 00 …(32B)  → layer=3 angle=135° (0x04) dpi=1600 (0x02)
```

括弧内が生のバイト値、その手前が対応表を通した表示値。

何も出ない場合の切り分け:

| `--debug` の状態 | 原因 | 対処 |
|---|---|---|
| 行が全く出ない | デバイスからレポートが来ていない | `doctor` でデバイス検出を確認。Keychron Launcher を閉じる |
| 行は出るが値が `-` | ルールのオフセットが合っていない | `learn -s 20` で再特定（ファーム更新時など） |
| 行も値も出るが画面に出ない | HUD の描画・位置 | `test` で確認。`hud.position` を `center` にして試す |

### 押したのに表示されないことがある

`rules[].notify` を `"always"` にすると、値が変わらなくても毎回表示する。

```json
"notify": "always"
```

既定は `"onChange"`（値が変わったときだけ）。各ボタンは必ず自分のバイトを変えるので
通常は `onChange` で取りこぼさないが、保険として `always` がある。

### `段 0x05（DPI 値 未設定）` と出る

DPI の段数が 5 段ではなかった。`rules[].dpi.map` にその段の DPI 値を追記する。

### `コード 0x2D（未校正）` と出る

向きの対応表に無いコードを受信した。`calibrate` で表を作り直す（§5）。

### 無関係なポップアップが出る

状態通知（cmd `0xA3`）に別種のイベントが混ざっている可能性がある。
`--debug` でレポート全文を確認し、`rules[].match` に条件を足して除外する。

```json
"match": [
  { "offset": 0, "equals": [163] },
  { "offset": 3, "equals": [2] }
]
```

`match` の全条件を満たすレポートだけがそのルールで処理される。
`equals` を省略して `mask` だけ書くと**何にでも一致してしまう**ので、`mask` は
`equals` と組で使う（`{ "offset": 1, "mask": 15, "equals": [1, 2, 3] }`）。

### アプリを起動しても何も起きない / すぐ終わる

メニューバーにアイコンが出ていなければ起動に失敗している。原因を見るには
バンドル内のバイナリを直接動かす:

```bash
/Applications/NapeHUD.app/Contents/MacOS/NapeHUD run
```

`kIOReturnNotPermitted` と出る場合は §9 の権限問題。
`device.usagePages` を既定の `[65376, 140]` に戻せば許可なしで動く。

### ファームウェア更新後に動かなくなった

レポート形式が変わった可能性がある。`learn` で再特定する。

```bash
nape-hud learn -s 20
```

3 フェーズの指示が出る（各 20 秒 / 全体 60 秒）。**Launcher は閉じておく。**

| フェーズ | やること |
|---|---|
| 1. 静止 | 何も触らない（トラックボールも動かさない） |
| 2. レイヤー切替ボタンのみ | レイヤー切替ボタンだけを数回押す |
| 3. 8 方向切替ボタンのみ | 8 方向切替ボタンだけを押す |

最後に `config.json` に貼れるルール雛形が出力される。

---

## 9. 必要な権限

**通常運用に追加の権限は不要。** 状態通知はベンダ面（`0xFF60` / `0x008C`）に来るので、
監視対象をそこに限れば macOS の許可なしで読み取れる（`device.usagePages` の既定値）。

| 権限 | 必要になる場面 |
|---|---|
| 入力監視 | `sniff -p` / `learn -p`、`usagePages` を空（全面）にしたとき、**ポインタ加速を `onlyTrackball: true` で使うとき**（§7） |
| アクセシビリティ | `keyFallback`（センチネルキー方式）、**ポインタ加速**（§7） |

現在の許可状況と監視面は `nape-hud doctor` で確認できる。

> ⚠️ **キーボード面を含めると許可が必須になる。**
> 未許可のまま含めると `IOHIDManagerOpen` 自体が `kIOReturnNotPermitted` で失敗し、
> レポートを 1 件も受信できなくなる（アプリ起動時はダイアログで案内が出る）。

### 権限はアプリ単位で管理される

`.app` として起動した場合、許可は **NapeHUD.app 自身**に紐づく。
ターミナルからバイナリを直接実行した場合は**ターミナルの許可**を使う。
そのため「ターミナルでは動くのにアプリでは動かない」ことが起こり得る。
`keyFallback` を使うなら、アプリをアクセシビリティに登録すること。

### ⚠️ 「許可済みなのに毎回聞かれる」場合

**ad-hoc 署名が原因。** ad-hoc だとアプリの識別子が
バイナリのハッシュ（`cdhash`）になるため、**再ビルドのたびに macOS が別アプリとみなす**。
システム設定の一覧に残っている項目は古いハッシュに紐づいた抜け殻で、新しいバイナリには効かない。

`make-app.sh` は**使える証明書があれば自動で署名に使う**
（`Developer ID Application` → `Apple Development` → `Mac Developer` の順）。
証明書で署名すると識別子が証明書ベースになり、再ビルドしても許可が維持される。

```bash
# 現在の識別子を確認する
codesign -d -r- /Applications/NapeHUD.app

# ad-hoc（毎回聞かれる）
# designated => cdhash H"78a69c8b31eb…"

# 証明書あり（許可が維持される）
# designated => identifier "com.local.nape-hud" and anchor apple generic and certificate leaf…
```

証明書を明示したい場合:

```bash
CODESIGN_IDENTITY="Developer ID Application: ..." ./scripts/make-app.sh --install
```

ad-hoc から証明書に切り替えた直後は、古い許可の残骸を消してから許可し直す:

```bash
tccutil reset Accessibility com.local.nape-hud
tccutil reset ListenEvent com.local.nape-hud
```

証明書が 1 枚も無い環境では ad-hoc になり、この問題は避けられない
（ビルド時に警告が出る）。

---

## 10. 付録: センチネルキー方式（keyFallback）

ファームウェアが状態を通知しなくなった場合の代替手段。既定では無効。

Keychron Launcher 側で各ボタンに `F13`〜`F20` のような普段使わないキーを併発させ、
それを本ツールが拾って表示する。接続方式に依存せず動く。

```json
"keyFallback": {
  "enabled": true,
  "consume": true,
  "bindings": [
    { "keyCode": 105, "layer": 1 },
    { "keyCode": 107, "layer": 2 },
    { "keyCode": 113, "angle": 0 },
    { "keyCode": 106, "angle": 90 },
    { "keyCode": 64,  "dpi": 1600 }
  ]
}
```

- `consume: true` … そのキーを他アプリに流さない（誤入力にならない）
- `layer` / `angle` / `dpi` … そのキーが押されたときに設定する値
- `modifiers` … `["cmd", "shift"]` のように修飾キーを要求できる
- macOS の仮想キーコード: `F13=105, F14=107, F15=113, F16=106, F17=64, F18=79, F19=80, F20=90`

この経路は**アクセシビリティ**権限が必要（§9）。

---

## 11. 付録: 設定ファイル全項目

```jsonc
{
  "device": {
    "vendorId": 13364,              // 0x3434 Keychron
    "productIds": [1088],           // 0x0440 Nape Pro
    "productNameContains": ["Nape"],// PID が違っても製品名で拾う
    "connectionNames": {},          // {"0x0441": "2.4GHz"} で接続種別を明示
    "usagePages": [65376, 140]      // 監視面。0xFF60 / 0x008C。空にすると全面（要 入力監視）
  },
  "hud": { /* §3 参照 */ },
  "layerNames": {},                 // レイヤー番号 → 表示名
  "angleNames": {},                 // 角度 → 表示名
  "dpiNames": {},                   // DPI 値 → 表示名（任意）
  "rules": [                        // 生レポート → 状態の復号規則
    {
      "name": "説明用の名前",
      "usagePage": 65376,           // 0xFF60
      "usage": 97,                  // 0x61
      "reportId": 0,
      "minLength": 4,
      "match": [{ "offset": 0, "equals": [163] }],   // cmd 0xA3
      "layer": { "offset": 1 },
      "angle": { "offset": 2, "map": { "1": 0, "2": 45 } },
      "dpi":   { "offset": 3, "map": { "0": 800, "1": 1200 } },
      "notify": "onChange"          // onChange | always
    }
  ],
  "calibration": {                  // 向きの校正（§5-3）
    "positions": 8,                 // 1 周あたりの方向数
    "angleZeroCode": null,          // 0° の基準にする生コード
    "clockwise": true               // 押す向きに対して角度を増やすか
  },
  "keymap": {                       // キーアサイン表示（§6）
    "layerCount": 8,                // 表示するレイヤー数。0 でデバイス申告値（VIA は 9 を返す）
    "keysPerLayer": 0               // 1 レイヤーのキー数。0 で自動判定
  },
  "acceleration": {                 // ポインタ加速（§7）。既定は無効
    "enabled": false,
    "onlyTrackball": true,
    "thresholdSpeed": 300,
    "fullSpeed": 2200,
    "baseGain": 1.0,
    "maxGain": 3.0
  },
  "keyFallback": { /* §10 参照 */ },
  "poll": {                         // 自発通知しないファーム向けの定期問い合わせ
    "enabled": false,
    "request": [1, 0, 0, 0],
    "intervalMs": 500
  }
}
```

### フィールド指定（`layer` / `angle` / `dpi` 共通）

| キー | 意味 |
|---|---|
| `offset` | 読み出すバイト位置 |
| `size` | バイト数（既定 1） |
| `encoding` | `value`（数値そのまま） / `bitmaskHighest` / `bitmaskLowest` |
| `bigEndian` | 複数バイトのとき `true` でビッグエンディアン |
| `add` / `multiply` | 生値の補正 `(raw + add) * multiply` |
| `map` | 生値 → 表示値の対応表。指定時は `add`/`multiply` より優先。**表に無い値は「未設定」として表示される** |

### `match` の条件

| キー | 意味 |
|---|---|
| `offset` | 照合するバイト位置 |
| `equals` | いずれかに一致すれば OK（配列） |
| `mask` | 照合前に `AND` するビットマスク |
